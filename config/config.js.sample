/* Config Sample
 *
 * For more information on how you can configure this file
 * see https://docs.magicmirror.builders/configuration/introduction.html
 * and https://docs.magicmirror.builders/modules/configuration.html
 *
 * You can use environment variables using a `config.js.template` file instead of `config.js`
 * which will be converted to `config.js` while starting. For more information
 * see https://docs.magicmirror.builders/configuration/introduction.html#enviromnent-variables
 */
let config = {
	address: "0.0.0.0",	// Listen on all interfaces so phones on Wi‑Fi can use the remote
	port: 8080,
	basePath: "/",	// The URL path where Lo Gandey is hosted. If you are using a Reverse proxy
									// you must set the sub path here. basePath must end with a /
	ipWhitelist: [],	// Set [] to allow all devices on your LAN (needed for QR remote access)
									// Or restrict to specific IPs, e.g. ["127.0.0.1", "::1", "192.168.1.42"],

	useHttps: false,			// Support HTTPS or not, default "false" will use HTTP
	httpsPrivateKey: "",	// HTTPS private key path, only require when useHttps is true
	httpsCertificate: "",	// HTTPS Certificate path, only require when useHttps is true

	language: "en",
	locale: "en-MV",   // Maldives locale (used by 3rd party modules)
			   // see https://en.wikipedia.org/wiki/Locale_(computer_software) for the possibilities

	logLevel: ["INFO", "LOG", "WARN", "ERROR"], // Add "DEBUG" for even more logging
	timeFormat: 24,
	units: "metric",
	zoom: 0.75,

	modules: [
		{
			module: "alert",
		},
		{
			module: "clock",
			position: "top_left",
			config: {
				timezone: "Indian/Maldives",
				lat: 4.1755,
				lon: 73.5093
			}
		},
		{
			module: "calendar",
			header: "Calendar",
			position: "top_left",
			config: {
				maximumEntries: 10,
				calendars: [
					{
						symbol: "calendar",
						name: "iCloud",
						url: "https://p157-caldav.icloud.com/published/2/YOUR-ICAL-FEED-ID"
					}
				]
			}
		},
		{
			module: "compliments",
			position: "lower_third"
		},
		{
			module: "weather",
			position: "top_right",
			config: {
				weatherProvider: "meteorologymv",
				station: "Hulhule",
				type: "current",
				lat: 4.1755,
				lon: 73.5093
			}
		},
		{
			module: "weather",
			position: "top_right",
			header: "Weather Forecast",
			config: {
				weatherProvider: "meteorologymv",
				station: "Hulhule",
				type: "forecast",
				lat: 4.1755,
				lon: 73.5093
			}
		},
		{
			module: "newsfeed",
			position: "bottom_bar",
			config: {
				showSourceTitle: true,
				showPublishDate: true,
				updateInterval: 30 * 1000,
				reloadInterval: 5 * 60 * 1000,
				ignoreOldItems: true,
				ignoreOlderThan: 5 * 24 * 60 * 60 * 1000,
				feeds: [
					{
						title: "TNN",
						url: "https://tnn.mv/en/rss/latest-posts"
					},
					{
						title: "TNN Local",
						url: "https://tnn.mv/en/rss/category/local-news"
					},
					{
						title: "adduLIVE",
						url: "https://www.addulive.com/feed"
					},
					{
						title: "Sun Online",
						url: "https://sun.mv/feed"
					},
					{
						title: "Vaguthu",
						url: "https://vaguthu.mv/feed"
					},
					{
						title: "VNews",
						url: "https://vnews.mv/rss"
					}
				],
				broadcastNewsFeeds: true,
				broadcastNewsUpdates: true
			}
		},
		{
			module: "remote-control",
			position: "bottom_right",
			config: {
				// Optional API key — generate with: node -e "console.log(require('crypto').randomUUID())"
				apiKey: "",
				secureEndpoints: false,
				showModuleApiMenu: true,
				showQRCode: true,
				qrCodeSize: 100,
				qrCodePosition: "replace",
				exclusiveModules: ["MMM-FIFAWorldCup"]
			}
		},
		{
			module: "MMM-FIFAWorldCup",
			position: "fullscreen_below",
			classes: "MMM-FIFAWorldCup",
			hiddenOnStartup: true,
			config: {
				title: "FIFA World Cup 2026",
				subtitle: "Knockout Bracket",
				showHero: true,
				showFooter: true,
				updateInterval: 3 * 60 * 1000,
				colored: true,
				showDates: true,
				animationSpeed: 1000
			}
		},
	]
};

/*************** DO NOT EDIT THE LINE BELOW ***************/
if (typeof module !== "undefined") { module.exports = config; }
